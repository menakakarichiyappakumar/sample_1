Jenkinsfile (Declarative Pipeline)
pipeline {
    agent {
        docker { image 'node:mysql' }
    }
    stages {
        stage('Test') {
            steps {
                sh 'node --version'
            }
        }
    }
}
